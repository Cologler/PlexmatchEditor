namespace PlexmatchEditor.Plexmatch;

/// <summary>
/// Beginning in Plex Media Server version 1.30.0, 
/// the scanner supports a hint type named pattern, 
/// or pt for short (both names are supported).
/// </summary>
/// <param name="Pattern"></param>
public record PlexmatchEpisodePatternRow(string Pattern)
{


}
